---
title: "Sandwich-Residuals: Parameter-Efficient Test-time Adaptation of World Models"
title_zh: Sandwich-Residuals：世界模型的参数高效测试时自适应
authors: "Krishnam Soni, Aditya Sehgal, Vedant Dave, Elmar Rueckert"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21740v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 基于自监督误差的世界模型参数高效测试时自适应
tldr: "潜空间世界模型能预测动作效果以支持规划，但测试条件偏离训练时预测会不可靠。现有测试时自适应常更新大量内部参数，还需选择适配组件。Sandwich-Residuals 保持模型冻结，仅在预测器周围学习小型残差修正，并用自监督预测误差在线优化，无需奖励、标签或源域数据。在 AdaJEPA 的 21 种条件下，成功率达冻结模型 1.3 倍，保留最强变体 95% 性能且少适配 97-99% 参数；复合偏移下达 1.9 倍。该工作表明有效测试时自适应未必需要修改预训练内部权重。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.21740v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 783, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.21740v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 998, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.21740v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1389, \"height\": 681, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.21740v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 1113, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21740v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 912, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21740v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1622, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21740v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 912, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21740v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 743, \"height\": 251, \"label\": \"Table\"}]"
motivation: 潜空间世界模型在测试条件与训练不同时预测不可靠，而现有测试时自适应需更新大量内部参数并选择适配组件，代价高且不灵活。
method: Sandwich-Residuals 冻结预训练世界模型，只在预测器周围学习小型残差修正，并用自监督预测误差在线优化，无需奖励、标签或源域数据。
result: "在 AdaJEPA 21 种条件下成功率达冻结模型 1.3 倍，保留最强变体 95% 性能且少适配 97-99% 参数；复合偏移下达 1.9 倍，并在 DINO-WM 三维操作任务验证。"
conclusion: 结果表明，世界模型的有效测试时自适应不必修改预训练内部权重，轻量残差即可在分布偏移下显著提升规划可靠性。
---

## 摘要
潜在世界模型通过在学习到的表示空间中预测动作的效果来支持规划，但当测试时条件与训练不同时，其预测可能变得不可靠。现有的测试时自适应方法通过更新预训练模型的部分参数来解决这一问题，通常需要修改数百万个参数，并且需要选择要自适应的内部组件。我们提出 Sandwich-Residuals，一种轻量级替代方案，它保持预训练世界模型冻结，仅围绕预测器学习小的残差修正。这些残差利用模型的自监督预测误差进行在线优化，不需要奖励、标签或源域数据。在 AdaJEPA 基准的 21 种条件下，我们的方法达到了冻结模型成功率的 1.3 倍，同时保留了最强 AdaJEPA 变体 95% 的性能，并且所需自适应的参数少了 97-99%。在复合偏移下，这一优势增加到冻结模型成功率的 1.9 倍，同时与内部块自适应保持相当。我们进一步在用于三维操作的 DINO-WM 模型上展示了相同的自适应原理。这些结果表明，世界模型的有效测试时自适应不一定需要修改其预训练的内部权重。

## Abstract
Latent world models enable planning by predicting the effects of actions in a learned representation space, but their predictions can become unreliable when test-time conditions differ from training. Existing test-time adaptation methods address this by updating parts of the pretrained model, often modifying millions of parameters and requiring a choice of which internal components to adapt. We introduce Sandwich-Residuals, a lightweight alternative that keeps the pretrained world model frozen and learns only small residual corrections around the predictor. The residuals are optimized online using the model's self-supervised prediction error and require no rewards, labels, or source-domain data. Across 21 conditions on the AdaJEPA benchmark, our method achieves $1.3\times$ the success rate of the frozen model while retaining 95% of the performance of the strongest AdaJEPA variant and adapting 97-99% fewer parameters. Under compound shifts, this advantage increases to $1.9\times$ the success rate of the frozen model, while remaining comparable to internal block adaptation. We further demonstrate the same adaptation principle on a DINO-WM model for 3-D manipulation. These results suggest that effective test-time adaptation of world models does not necessarily require modifying their pretrained internal weights.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
- **研究动机**：潜空间世界模型（如 JEPA、DINO-WM）能在学习到的表示空间中预测动作后果并支持规划，但其前提是测试条件与训练分布接近。真实机器人场景中常出现外观偏移（光照、背景、视角、颜色）和动力学偏移（摩擦、质量、控制器增益、负载等），导致预测不可靠、下游控制性能下降。
- **现有方法问题**：AdaJEPA 等测试时自适应方法会在线更新预训练模型内部权重，通常修改数百万参数，并且需要选择适配哪个预测器块或编码器组件；最佳适配目标还依赖环境，代价高且不够灵活。
- **核心问题**：有效的测试时自适应是否必须修改预训练世界模型内部权重？能否只通过轻量接口级修正来恢复规划性能？
- **整体含义**：论文提出 Sandwich-Residuals，将预训练编码器和预测器全部冻结，仅在预测器输入和输出两侧学习小型残差修正。结果表明，世界模型的有效测试时自适应不一定需要改动内部权重，接口级残差即可在多种分布偏移下显著提升规划可靠性。

### 2. 方法论：核心思想与关键技术细节
- **核心思想**：在冻结的预测器前后形成“三明治”残差结构。输入侧残差修正视觉、动作嵌入，输出侧残差修正预测的视觉和本体感知潜变量；仅这些残差模块在线更新。
- **输入侧修正**：
  - 动作嵌入修正：`êza = za + ra([za; zp])`，动作残差以动作嵌入和本体感知状态为条件，使同一动作可在不同系统状态下被不同修正。
  - 视觉嵌入修正：`êzv = zv + rv,in(zv)`，对每张编码图像施加残差，包括上下文帧、观测目标帧和规划目标图像。DINO-WM 中对每个 patch token 施加。
  - 本体感知输入嵌入不修正；论文假设本体感知观测映射不变。
  - 修正后预测器输入：`êx = [êzv; zp; êza]`。
- **预测与输出侧修正**：
  - 冻结预测器产生 `x̂ = fΘ(êx)`。
  - 输出视觉残差：`êẑv = ẑv + rv,out(ẑv)`。
  - 输出本体感知残差：`êẑp = ẑp + rp,out(ẑp)`。
  - 修正后的表示用于递归 rollout 和潜空间规划目标。
- **残差结构**：
  - 视觉输入/输出残差为两层 MLP：`R384 → R64 → R384`，GELU 激活。
  - AdaJEPA 动作输入残差为线性 `R20 → R10`，本体感知输出残差为线性 `R10 → R10`。
  - AdaJEPA 总可训练参数 99,520，占其更新参数量的 1.0%（迷宫模型 9.93M）和 2.7%（推动模型 3.66M）。
  - DINO-WM 动作残差 `R10 → R10`，无本体感知输出残差，总可训练参数 99,310，占 AdaJEPA 更新量 3.23M 的 3.1%。
- **在线优化目标**：
  - 使用自监督潜预测误差：预测观测表示与编码观测表示之间的 MSE，目标侧应用 stop-gradient。
  - 缓冲最近 5 个 transition，损失在最多 K 个连续 transition 窗口上平均。
  - 每个 plan-execute 周期后执行一次 Adam 更新：`φ ← φ − ηφ ∇φ Lpred`。
  - 线性残差学习率 `1e-2`，视觉 MLP 学习率 `5e-4`。
- **初始化与重置**：
  - 线性残差初始化为零，MLP 最后一层初始化为 `N(0, 1e-4)`，因此每 episode 开始时 adapted 模型与冻结预训练模型一致。
  - 残差参数在 episode 内跨 replanning 周期保留，每个新 episode 重置。
- **无需**：奖励、标签、源域数据；也无需选择要更新的预测器块。

### 3. 实验设计
- **AdaJEPA 基准**：
  - 使用 AdaJEPA 发布的 checkpoint、环境和 planner，仅增加残差模块。
  - 四个目标条件视觉控制任务：
    - **Medium Maze**：MuJoCo 点质量智能体在固定迷宫导航。
    - **Diverse Maze**：同一智能体在未见布局上评估。
    - **PushT**：pymunk 推动器将 T 形块推到目标位姿，仅用 T 块训练。
    - **PushObj**：同一推动器，模型在 `{T, L, Z, +}` 四种形状上训练。
  - 测试时偏移：
    - 动力学：关节阻尼 `×50`、身体密度 `0.2–10×`、PushT 的 PD 控制器增益 `kv` 加倍。
    - 外观：高斯模糊 `σ=2`、椒盐噪声 `1%`、亮度 `0.9×`、智能体/块/目标锚点变红。
    - 任务：PushObj 在未见形状 `{I, small T, square}` 上评估；Diverse Maze 使用未见布局。
    - 复合偏移：7 种外观损坏与动力学变化组合。
  - 共 **21 个主条件 + 7 个复合条件**。
  - 规划协议：每 5 个仿真步观察一次，上下文 `K=3`；CEM 优化 5 个动作块，200 候选、30 精英、
